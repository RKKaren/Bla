'use strict';

const upper = (str) => {
  return str.toUpperCase();
}
const poqr = (str) => {
  return str.toLowerCase();
}
const PORT = 8080;

module.exports = {
  upper,
  poqr,
  PORT,
  something: 5
}
